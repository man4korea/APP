# 📁 C:\xampp\htdocs\BPM\CLAUDE_CODE_SETUP_PACKAGE\config\environment_variables.env
# Create at 2508031210 Ver1.00

# ========================================
# BPM 프로젝트 환경 변수 설정
# ========================================

# 기본 프로젝트 정보
PROJECT_NAME=BPM Total Business Process Management
PROJECT_VERSION=1.0.0
ENVIRONMENT=development

# 데이터베이스 설정
DB_CONNECTION=mysql
DB_HOST=localhost
DB_PORT=3306
DB_DATABASE=bpm_database
DB_USERNAME=root
DB_PASSWORD=

# Redis 캐시 설정
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DATABASE=0
REDIS_TIMEOUT=2.5

# JWT 및 보안 설정
APP_KEY=BPM_SECRET_KEY_2025
JWT_SECRET=JWT_SECRET_KEY_FOR_BPM
JWT_TTL=60
JWT_REFRESH_TTL=20160

# 세션 설정
SESSION_DRIVER=redis
SESSION_LIFETIME=120
SESSION_ENCRYPT=true

# 캐시 설정
CACHE_DRIVER=redis
CACHE_PREFIX=bpm_cache

# 파일 업로드 설정
UPLOAD_MAX_SIZE=10240
ALLOWED_FILE_TYPES=jpg,jpeg,png,gif,pdf,doc,docx,xls,xlsx,ppt,pptx
UPLOAD_PATH=uploads/

# 메일 설정 (선택사항)
MAIL_MAILER=smtp
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=
MAIL_PASSWORD=
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS=noreply@bpm.example.com
MAIL_FROM_NAME="BPM System"

# API 키들 (필요한 경우 설정)
GITHUB_TOKEN=
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# 로깅 설정
LOG_CHANNEL=stack
LOG_LEVEL=debug
LOG_PATH=logs/

# 배포 경로 설정
LOCAL_PATH=C:\\xampp\\htdocs\\BPM
ONEDRIVE_PATH=C:\\Users\\man4k\\OneDrive\\문서\\APP\\bpm
WEBSERVER_PATH=Z:\\html\\bpm

# SuperClaude 설정
SUPERCLAUDE_ENABLED=true
SUPERCLAUDE_AUTO_COMMIT=false
SUPERCLAUDE_REVIEW_LEVEL=thorough

# MCP 서버 설정
MCP_ENABLED=true
MCP_SHRIMP_ENABLED=true
MCP_FILESYSTEM_ENABLED=true
MCP_GITHUB_ENABLED=true

# 개발 도구 설정
DEBUG=true
ENABLE_QUERY_LOG=true
ENABLE_PERFORMANCE_MONITORING=true

# Playwright/Puppeteer 설정
BROWSER_HEADLESS=true
BROWSER_TIMEOUT=30000
SCREENSHOT_PATH=tests/screenshots/

# 보안 설정
CSRF_PROTECTION=true
XSS_PROTECTION=true
RATE_LIMITING=true
RATE_LIMIT_PER_MINUTE=60

# 멀티테넌트 설정
MULTI_TENANT=true
DEFAULT_TENANT=default
TENANT_CACHE_TTL=3600

# ========================================
# 환경별 설정 오버라이드
# ========================================

# 개발 환경 (development)
# - 디버그 모드 활성화
# - 상세한 오류 메시지 표시
# - 캐시 무효화

# 스테이징 환경 (staging)  
# - 디버그 모드 비활성화
# - 기본 캐시 활성화
# - 제한적 로깅

# 프로덕션 환경 (production)
# - 모든 디버그 정보 비활성화  
# - 최대 캐시 활성화
# - 최소 로깅

# ========================================
# 사용법 안내
# ========================================

# 1. 이 파일을 .env로 복사
# cp config/environment_variables.env .env

# 2. 실제 값들로 교체
# - 데이터베이스 비밀번호
# - API 키들
# - 메일 설정 등

# 3. 보안을 위해 .env 파일은 Git에 커밋하지 않음
# (.gitignore에 .env 추가됨)

# 4. 배포시에는 각 환경에 맞는 .env 파일을 별도 관리

# ========================================
# 주의사항
# ========================================

# ❌ 절대 실제 비밀번호나 API 키를 이 파일에 넣지 마세요
# ✅ 이 파일은 템플릿용이며, 실제 .env 파일에 값을 입력하세요
# ✅ .env 파일은 서버별로 다른 값을 가질 수 있습니다
# ✅ 중요한 정보는 암호화하여 저장하세요